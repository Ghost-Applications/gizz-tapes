import kotlin.time.Duration;

CREATE TABLE files (
  recordingId TEXT NOT NULL,
  filePath TEXT NOT NULL,
  length INTEGER AS Duration NOT NULL,
  title TEXT NOT NULL,

FOREIGN KEY(recordingId) REFERENCES recordings(id)
);

insertFile:
INSERT INTO files VALUES ?;
